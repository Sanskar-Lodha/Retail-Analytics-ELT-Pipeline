# Dataset

The raw retail dataset is generated using `dataset_generation_script.py`.

The generated JSON file is intentionally excluded from this repository because it exceeds GitHub's file size limit (100 MB).

Run the script to generate the dataset locally before executing the dbt pipeline.